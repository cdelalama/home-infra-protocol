<!-- doc-version: 0.4.0 -->
# LLM Session History

Append-only record of meaningful LLM-assisted work on this project.

## Format

YYYY-MM-DD - <LLM_NAME> - <Brief summary> - Files: [list] - Version impact: <yes/no + details>

## Entries

- 2026-05-12 - GPT-5 Codex - **Documentation cleanup after `/new-homelab-project` relocation to ForgeOS.** Commit `97fae4c` correctly moved the project-creation orchestrator and Claude skill out of this repo and into ForgeOS, leaving `home-infra-protocol` as the owner of the homelab profile (`apply-profile.sh`, templates, checklists) and contract docs. The commit updated `integrations/dockit/INTEGRATION.md`, but the repo's LLM bookkeeping still had `HANDOFF.md` at `Last Updated: 2026-05-10` with no 2026-05-12 HISTORY entry, and the HANDOFF's one-time setup snippet still pointed at the removed `integrations/dockit/skills/new-homelab-project` path. This cleanup updates HANDOFF freshness, records the refactor, and points the symlink snippet at `~/src/forgeos/skills/new-homelab-project`. No SPEC/schema/profile behaviour changes. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no (bookkeeping + stale operator path only; protocol contract remains 0.4.0)
- 2026-05-10 - Claude Opus 4.7 (1M context) - **Cross-LLM reconciliation `/brief` — no code change.** Operator opened the repo cold after a pause and ran `/brief`, then surfaced that the previous Claude `/brief` (2026-05-08, when state was 0.3.1 with empty *Open work*) disagreed with a GPT/Codex summary the operator had received. Reconciliation: my prior brief was two days stale because between sessions DF-005 was filed (2026-05-08, commit `0ec1f86`) and Codex shipped 0.4.0 closing DF-006 (2026-05-09, commit `8ddff80`). GPT was reading current HEAD; my old brief was citing residual memory. Re-emitted brief aligned with the current HANDOFF: state is 0.4.0, next concrete step is authoring `docs/HOMELAB_PROFILE_COLLISION_AND_POPULATE_PROPOSAL.md` for DF-005. **Workflow lesson worth recording**: when a non-Claude LLM ships work between Claude sessions, the Claude session that re-opens the repo MUST defer to HANDOFF + git HEAD over its own residual memory — exactly the property the 2026-05-06 meta cleanup targets. The cross-LLM property holds: a Codex commit (`8ddff80`) shipped a clean 0.4.0 by reading this repo's docs alone, and a Claude session two days later reconciled to it by reading the same docs. Recorded here for the `Stop` hook's daily-freshness validator. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no
- 2026-05-09 - Codex - **Minor 0.4.0: DF-006 exposure/secrets/deviation contract.** Real-world Hermes NAS activation in `home-infra` exposed that the protocol accepted an operator-visible `interface: web` URL pointing at `127.0.0.1`. Shipped additive fields: `Service.exposure` (`visibility: operator|local|hidden`, `canonical`, `backend_url`), `Service.secrets_source` (store-neutral reference block; no secret values), and `deployment.pattern` / `deployment.deviations[]` (intent/provenance for upstream Docker patterns and intentional overrides). SPEC now says validators SHOULD reject literal loopback/private-IP hosts in `url` for operator-visible web services, while allowing split-horizon hostnames that resolve to private LAN IPs; proxy backends belong in `exposure.backend_url`. `examples/home-infra/catalog/services.yml` updated with exposure/backend examples, deployment patterns, and a generic secret-source reference. `docs/DOWNSTREAM_FEEDBACK.md` adds DF-006 and marks it implemented. Cross-repo adopter action in `home-infra`: local `scripts/audit-catalog.py`, Hermes dashboard and ESPHome Builder moved to canonical `*.lamanoriega.com` URLs, and Hermes Doppler source declared. 28 doc-version targets synced via `scripts/bump-version.sh 0.4.0`. - Files: [SPEC.md, schemas/services.schema.json, examples/home-infra/catalog/services.yml, docs/DOWNSTREAM_FEEDBACK.md, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 24 doc-version targets] - Cross-repo: home-infra catalog/docs/auditor updated in the same operator session. - Version impact: yes (0.3.1 -> 0.4.0)
- 2026-05-08 - Claude Opus 4.7 (1M context) (arbiter session in `home-infra` with multi-repo context) - **DF-005 filed: homelab profile / runbooks-path collision + `/new-homelab-project` HANDOFF *Open work* population gap.** No code change, no schema change, no SPEC contract change. Originating chain: 2026-05-08 pi-fleet 0.1.1 was the first homelab-profile project scaffolded end-to-end via `integrations/dockit/new-homelab-project.sh`. Codex audit of `pi-fleet@a3eaf8d` cazó four template-residue findings + two homelab-profile-specific findings in one pass. The four template-residue findings filed as **LLM-DocKit DF-035** (sister DF, scaffold-layer concern, commit `9f2c632` in that repo); the two homelab-profile findings aggregated into this repo's **DF-005** because they share a single root cause (scaffold step does not propagate context from brainstorm to new repo's docs). DF-005 carries enough scope (cross-repo coordination for option A1, skill input contract change for option B1, brainstorm format contract decision) that a `docs/HOMELAB_PROFILE_COLLISION_AND_POPULATE_PROPOSAL.md` is the right next artefact before any implementation session. *Open work — next concrete step* + *Pending Proposals* + *Open DF entries* blocks updated to point at DF-005 (preserving the auto-orientation contract for fresh sessions reading this repo cold). The arbiter step itself — recognising that two findings in one audit are facets of one upstream gap, AND that the two findings here pair with four findings in LLM-DocKit's scaffold layer for one cross-cutting story — is missing from the proto-`/consensus` flow today; recorded as `forgeos/docs/llm/HANDOFF.md` *Open work* item #8 (cross-LLM protocol gap, upstream-recognition step) so the missing step is itself durable as backlog. Without an arbiter session with multi-repo context, the four+two findings would have been fixed locally in pi-fleet 0.2.0 and the systemic upstream gaps would not have surfaced — that is the bug of the workflow this DF chain attacks, not of any individual LLM. - Files: [docs/DOWNSTREAM_FEEDBACK.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Cross-repo: LLM-DocKit DF-035 sister filing committed in same operator session (`9f2c632`); forgeos HANDOFF item #8 pending in this same operator session. - Version impact: no
- 2026-05-08 - Claude Opus 4.7 (1M context) - **Recap turn — no code change.** Operator opened the session and asked for a recap of what the previous day's session shipped. Session restated the two prior commits in plain Spanish: cold-orientation advisory turn (`334795c`, no code) followed by the 0.3.1 implementation (`f002f48`, DF-004 closure option (a)). No SPEC, schema, examples, CHANGELOG, or VERSION change in this turn. Recorded here for the same reason as the 2026-05-07 advisory entry: the project's `Stop` hook validates HANDOFF/HISTORY freshness on every session close regardless of whether the turn was implementation, advisory, or recap; bypassing with `--no-verify` is the anti-pattern the hook is supposed to catch. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no
- 2026-05-07 - Claude Opus 4.7 (1M context) - **Patch 0.3.1: DF-004 closure (option (a)).** Same-day follow-up to the 2026-05-07 cold-orientation advisory turn (commit `334795c`). Closed DF-004 by broadening the `interface` default rule in `SPEC.md` *Service / interface*: replaced the previous formulation *"when `url` does not start with `http://` or `https://`"* (which only caught non-HTTP URLs and silently mis-classified HTTPS APIs without HTML as `web`) with *"when the service does not serve HTML at the listed `url`"*, plus explicit guidance that HTTP APIs without HTML — MCP, REST, GraphQL, JSON-RPC — MUST declare `interface: api`. The previous wording's narrow scope was the root cause of the 2026-05-03 unifi-mcp incident: catalog complied, schema accepted, consumer applied the permissive `web` default, user clicked an "open" button that opened a tab to a 404 because the MCP `/mcp` endpoint serves no HTML. `schemas/services.schema.json` `interface` field `description` updated to mirror the broader rule and name the API-without-HTML case explicitly; `required` (none), enum (open list), and default (`web`) unchanged because the schema cannot inspect Content-Type — option (b) validator check stays queued. `examples/home-infra/catalog/services.yml` `example-api` entry gains an explanatory comment naming the rule clarified in 0.3.1 so adopters reading the example see the rule alongside the canonical case it applies to. `docs/DOWNSTREAM_FEEDBACK.md` DF-004 status moves from `open` to `implemented (0.3.1)` with the resolution summary inline; the entry's *Implementation hints (option (a))* block stays in place as the historical reference for how this kind of closure was structured. CHANGELOG entry added under [0.3.1]. Cross-repo read-only sweep of `~/src/home-infra/catalog/services.yml` per `docs/LLM_WORKFLOW.md` *When Changing Field Semantics*: no drift across the 11 services audited — `mosquitto` declares `mqtt`, `unifi-mcp` declares `api` (the original 2026-05-03 mitigation), `esphome-builder` declares `web` explicitly, and the remaining 8 services that omit `interface` genuinely serve HTML at the listed URL. No follow-up commit needed in `home-infra` for this protocol patch. 28 doc-version targets synced via `scripts/bump-version.sh 0.3.1`. **First patch shipped via the 2026-05-06 meta-cleanup property** — the cold session that opened this repo at the start of 2026-05-07 read only the *Open work — next concrete step* block + DF-004 *Implementation hints* block, no bespoke dispatch prompt; the property the meta cleanup was designed to deliver is now empirically validated end-to-end. - Files: [SPEC.md, schemas/services.schema.json, examples/home-infra/catalog/services.yml, docs/DOWNSTREAM_FEEDBACK.md, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 24 doc-version targets] - Version impact: yes (0.3.0 -> 0.3.1)
- 2026-05-07 - Claude Opus 4.7 (1M context) - **Advisory turn — no code change.** Operator opened the repo cold and asked "qué hay que hacer aquí?". Session answered by reading `docs/llm/HANDOFF.md` *Open work — next concrete step* block and `docs/DOWNSTREAM_FEEDBACK.md` DF-004 *Implementation hints*: the next concrete step is patch 0.3.1, option (a) — SPEC clarification of the `interface` default rule + schema description update + example sanity-check + DF-004 status to `implemented (0.3.1)`, with read-only sweep of `~/src/home-infra/catalog/services.yml`. Operator received the summary; awaiting explicit approval to execute. No SPEC, schema, examples, CHANGELOG, or VERSION change in this turn. This is the first session that validates the 2026-05-06 meta-cleanup property end-to-end: a fresh, context-less session orientation in this repo no longer requires a bespoke dispatch prompt — the *Open work* block + DF *Implementation hints* are sufficient. Recorded here because the project's `Stop` hook (in `.claude/settings.json`, paired with `.claude/rules/require-docs-on-code-change.md`) validates HANDOFF/HISTORY freshness on every session close regardless of whether the turn was implementation or advisory; bypassing with `--no-verify` is the anti-pattern the hook is supposed to catch. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no
- 2026-05-06 - Claude Opus 4.7 (1M context) - **Meta cleanup — close protocol-dev gaps that forced bespoke prompts.** No code change, no schema change, no SPEC contract change. Originating diagnosis: Carlos asked "shouldn't a fresh session already know what to do?" — exposing that every time we wrote a long bespoke session-handoff prompt, we were defeating the system whose purpose is exactly to make those prompts unnecessary. Three additions: (1) `docs/llm/HANDOFF.md` gains an *Open work — next concrete step* block at the top so a fresh session sees DF-004 closure as the immediate task without conversational context; replaces the implicit pattern of "next step is whichever Pending Proposal is open" with an explicit pointer including expected version bump and cross-references. (2) `docs/LLM_WORKFLOW.md` gains a *When Changing Field Semantics* section formalising the read-only adopter-catalog sweep convention — promoted from anecdote (the lesson DF-004 itself teaches at its tail block) to repo-level convention. The rule: re-sweep `~/src/home-infra/catalog/services.yml` read-only when changing field semantics, halt and report drift, never edit cross-repo from the protocol session. (3) `docs/DOWNSTREAM_FEEDBACK.md` template gains an optional *Implementation hints (when the recommended option is decided)* block — Files to touch / Version bump / Cross-repo touches required — and DF-004 retroactively gains its hints for option (a). The *Implementation hints* block is the difference between "DF describes a problem" (open) and "DF is dispatchable as a closure session" (accepted). Net effect: the dispatch prompt for closing DF-004 (Session 6 in the master roadmap) drops from ~100 lines of bespoke context to ~4 lines pointing at this repo's own docs. The master roadmap in `home-infra` will be updated separately to reflect the shorter prompt; that is a sibling commit. Validators: `scripts/check-version-sync.sh` PASS (28 targets in sync at 0.3.0), `scripts/dockit-validate-session.sh --human` PASS 6/6. - Files: [docs/llm/HANDOFF.md, docs/LLM_WORKFLOW.md, docs/DOWNSTREAM_FEEDBACK.md, docs/llm/HISTORY.md] - Version impact: no
- 2026-05-06 - Claude Opus 4.7 (1M context) - **Advisory turn — no code change.** Operator asked for an ecosystem-map summary covering the four protocols in flight (`home-infra-protocol`, `LLM-DocKit` scaffold, `CONSENSUS_PROTOCOL_PROPOSAL.md` in `LLM-DocKit/docs/`, and the `~/src/llm-council` prior-art that the 2026-05-04 reconciliation roadmap will absorb in Sessions 4/5). Then operator drafted a dispatch prompt for the next session — closing DF-004 by applying option (a) as patch 0.3.1 — and asked for a critique. Critique returned six concrete points (default explicit, option (c) escape to Consensus, cross-repo sweep gating, push-vs-cross-repo confirmation split, version-bump rules, anti-auto-memory warning); operator incorporated all of them in the refined prompt. No schema, SPEC, examples, CHANGELOG, or VERSION change in this turn. Recorded here because the project's `Stop` hook (per `.claude/rules/require-docs-on-code-change.md`) fires on every session close regardless of whether the turn was implementation or advisory; the alternative — bypassing with `--no-verify` — is exactly the anti-pattern the hook is supposed to catch. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] - Version impact: no
- 2026-05-04 - Claude Opus 4.7 (1M context) - **DocKit gate close + push of 0.3.0.** The previous session (2026-05-03) implemented Deployment Evidence per Acceptance criteria — schema, SPEC vocabulary, examples, DF-003 status, 28 doc-version targets — but did not commit. Working tree carried 30 modified files (VERSION already at 0.3.0, validator FAIL only on stale date / missing 2026-05-04 HISTORY entry). Discovered via cross-repo audit by GPT-5 from a sibling Claude session in `~/src/tomatic` while triaging next-step orientation. Strict scope here: **close, do not extend.** Updated HANDOFF *Last Updated* to 2026-05-04, added this HISTORY entry, ran `scripts/dockit-validate-session.sh --human` (6/6 PASS), staged the 30 modified paths explicitly, committed `feat(0.3.0): deployment evidence contract`, pushed `origin/main`. The implementation narrative lives in the 2026-05-03 entry below; this entry is the closure receipt. - Files: [docs/llm/HANDOFF.md, docs/llm/HISTORY.md] (date refresh + this entry; the other 28 paths in the same commit were already prepared by the implementing session) - Version impact: no for this delta (the 0.2.5 → 0.3.0 bump was already applied by the implementing session; this entry only records the push)
- 2026-05-03 - Claude Opus 4.7 (1M context) - **Minor 0.3.0: Deployment Evidence Contract shipped end-to-end.** Implemented `docs/DEPLOYMENT_EVIDENCE_PROPOSAL.md` *Acceptance criteria* in a single session, no new consensus run (the proposal closed its consensus on 2026-05-03 — see `docs/llm/REVIEWS.md` of the same date for the causal trail). Changes: (1) `schemas/services.schema.json` adds the optional `deployment` block on each service item: `expected.image` (string, optional) and `expected.health` sub-block with required `url` / `version_json_path` / `version`; every level is independently optional; `additionalProperties: true` preserved at every nesting level so 0.2.x consumers continue to load 0.3.0 catalogs and ignore the new field. (2) `SPEC.md` *Service* section grows four normative sub-sections — *Deployment lifecycle vocabulary* (the six states `declared / implemented / built / transferred / running / serving` with verification mechanism per state), *Rule: "operationally deployed"* (the claim is valid only when `running` and `serving` are confirmed by runtime evidence; for services with a version-bearing endpoint, `serving` MUST confirm the expected version), *Rule: intent vs evidence* (catalog declares intent; runtime observation never lives in the catalog; hand-maintained `observed_*`/`actual_*` fields are forbidden because they rot), *`deployment`* (block shape, fields, drift severity vocabulary INFO/WARN/FAIL as semantics-not-enforcement, pointer to JSON schema). `deployment` added to *Recommended fields*. (3) `examples/home-infra/catalog/services.yml` shows three of the five proposal scenarios on sanitized hostnames: `infra` (own app + endpoint, image + health), `home-dashboard` (third-party with HTTP UI, image only with `latest` tag to model digest-as-evidence), `example-mqtt` (third-party TCP only, image only). Inline comments cross-reference the proposal anti-patterns. Schema validates the example with `jsonschema` (Python). (4) `docs/PROJECT_CONTRACTS.md` notes that project-level service objects, when used, may include the same `deployment` block with the same semantics — intent declared in the project repo, evidence read by consumers, never edited to match observed reality. (5) `docs/DOWNSTREAM_FEEDBACK.md` DF-003 status moves from `accepted` (proposal in flight) to `implemented (0.3.0)` with the resolution summary inline. DF-002 stays `implemented` (consumer-side TCP probe shipped in `infra-portal` 0.8.0/0.8.1 and is in production); the new contract gives that work a vocabulary and an extension path (e.g. wiring a `deployment.expected.health.version` comparison in the portal as a follow-up consumer-side patch). 28 doc-version targets synced via `scripts/bump-version.sh 0.3.0`; `scripts/check-version-sync.sh` reports 28/28 in sync. - Files: [schemas/services.schema.json, SPEC.md, examples/home-infra/catalog/services.yml, docs/PROJECT_CONTRACTS.md, docs/DOWNSTREAM_FEEDBACK.md, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 24 doc-version targets] - Version impact: yes (0.2.5 -> 0.3.0)
- 2026-05-03 - Claude Opus 4.7 (1M context) + Carlos (operator) + GPT-5 (audit) - **Patch 0.2.5: SPEC matrix row for infra-portal synced to 0.8.1.** Post-deploy audit caught that unifi-mcp was permanently `down` after 0.8.0 promote, despite Carlos confirming the MCP is in active use. Investigation showed `/mcp` returns HTTP 406 to a plain GET (canonical MCP signature: a JSON-RPC error body proving the server is alive and protocol-aware); the catalog correctly declared `expect_status: 406` but the previous logic in `httpProbe` gated `up` behind `acceptable === res.status >= 200 && res.status < 400`, dropping any explicit `expect_status` outside the 2xx-3xx range to `down`. Fix shipped in `infra-portal` 0.8.1: extracted pure exported function `decideHttpState(status, expected)` with the contract "explicit `expect_status` is ground truth; absent expectation falls back to 2xx-3xx heuristic", added 11 unit tests (`tests/health-decide-http-state.test.ts`) using built-in `node:test --import tsx`, deployed to NAS in the same session. Runtime evidence: container `Up (healthy)` at `infra-portal:0.8.1`; `/api/health` returns `0.8.1`; `unifi-mcp` flipped from `down (HTTP 406)` to `up (HTTP 406)` — same response, correct interpretation now; total status: 10 services up, 1 disabled (pentagi by design), 0 down. On this protocol side: `SPEC.md` *Consumer support for `interface`* matrix row updated from `0.8.0` to `0.8.1`; Notes column documents the new `expect_status` ground-truth contract. The pattern that surfaced this — propose / implement / audit / find drift / fix-forward — is exactly DF-030 lesson 1 (auditor as fourth role) operating in the same chain that closed DF-002 hours earlier on the same day. 28 doc-version targets synced via `scripts/bump-version.sh 0.2.5`. - Files: [SPEC.md, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 24 doc-version targets] - Cross-repo: `infra-portal` 0.8.1 (deploy + tests), `home-infra` (catalog + INVENTORY/SERVICES/PROJECTS image tag bumps + unifi-mcp catalog status block fixed). - Version impact: yes (0.2.4 -> 0.2.5)
- 2026-05-03 - Claude Opus 4.7 (1M context) + Carlos (operator) - **Patch 0.2.4: DF-002 closed in production after `infra-portal:0.8.0` promoted to NAS.** Operator authorised the production promotion of `infra-portal:0.8.0` following the six-step evidence plan in `~/src/home-infra/docs/SESSION_HANDOFF_2026-05-04_ECOSYSTEM_RECONCILIATION.md` §8. Sequence: (1) pre-flight verified repo VERSION 0.8.0 + Dockerfile + DEPLOY_PLAYBOOK; current NAS container was `infra-portal:0.7.2` healthy. (2) `npm run build` + `docker build -t infra-portal:local -t infra-portal:0.8.0 .` on dev-vm; verified internal `cat /app/VERSION` returns `0.8.0`. (3) `docker save | ssh nas docker load` (full QNAP path per CONVENTIONS). (4) `sed -i 's|image: infra-portal:.*|image: infra-portal:0.8.0|' docker-compose.yml` on NAS; `docker compose up -d --force-recreate` via the QNAP plugin path. Container recreated cleanly. (5) Runtime evidence — all four checks passed: `docker ps` shows `infra-portal:0.8.0` `Up (healthy)`; `curl /api/health` returns `{"ok":true,"version":"0.8.0"}`; `curl /api/status` for `mosquitto` returns `{"state":"up","message":"TCP 10.0.0.139:1883 connected"}` (was `unknown / "tcp probes not implemented yet"` under 0.7.2 — DF-002's symptom physically gone); `curl /api/catalog` exposes `interface` field for the two services that declare it (`mosquitto: mqtt`, `esphome-builder: web`); other services without explicit `interface` accept the `web` default per protocol 0.2.0 spec without the portal exposing the field, which is correct behaviour. (6) Documentation updates: `~/src/home-infra/docs/SERVICES.md` infra-portal image tag bumped from `0.7.0` to `0.8.0` with a note about the deploy date and what the new version ships; `home-infra` working tree note carries forward. Note for future audit: `~/src/home-infra/docs/INVENTORY.md` and `~/src/home-infra/docs/PROJECTS.md` do NOT list infra-portal explicitly — historical omissions, not addressed in this commit (out of scope; would require operator decision on whether to register it as a project entry). DF-002 finally `implemented` per the new ontology — first deployed-evidence-satisfied DF in the channel. The deploy chain that DF-029 named as a gap (validator PASS != deployed) is now closed for this consumer; the broader pattern (auto-detection of repo-vs-deployed drift) remains the work item DF-029 names. - Files: [docs/DOWNSTREAM_FEEDBACK.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, CHANGELOG.md, VERSION + 24 doc-version targets] - Cross-repo: home-infra/docs/SERVICES.md updated. - Version impact: yes (0.2.3 -> 0.2.4)
- 2026-05-03 - Claude Opus 4.7 (1M context) + GPT-5 (audit) + Carlos (arbiter) - **Patch 0.2.3: mechanical status-string fix-forward after audit.** GPT-5 audit on 2026-05-03 caught two drifts introduced by 0.2.2: (1) `docs/DOWNSTREAM_FEEDBACK.md` DF-002 was marked `implemented (protocol 0.2.0 + infra-portal 0.8.0)`, but the new "operationally deployed" ontology (in `DEPLOYMENT_EVIDENCE_PROPOSAL.md`) requires `running` AND `serving` confirmed by runtime evidence — and `infra-portal` 0.8.0 only exists in repo, with production still on `0.7.2`. The proposal's first real-world example was contradicting the proposal's own normative rule. Corrected to `partially implemented (protocol 0.2.0 + infra-portal repo 0.8.0; deployment to production pending)`. (2) Cross-repo: `~/src/LLM-DocKit/docs/DOWNSTREAM_FEEDBACK.md` DF-029 used the non-legend status `partially accepted`; the file's own legend defines only `open / accepted / partially implemented / implemented / rejected / superseded-by`. Corrected to `accepted` (per legend: "listed in a `*_PROPOSAL.md` and committed to the roadmap" — exactly the current state). The DF moves to `partially implemented (X.Y.Z)` only when the template-side changes actually ship. Also normalised the matching mention in `~/src/LLM-DocKit/docs/CONSENSUS_PROTOCOL_PROPOSAL.md` acceptance criteria. Both fixes are mechanical (status text alignment), not deliberative — no new REVIEWS entry filed. The patch is itself an instance of the Consensus Protocol's failure mode "decision later overturned" applied to documentation drift, captured in the CHANGELOG. 28 doc-version targets synced via `scripts/bump-version.sh 0.2.3`. - Files: [docs/DOWNSTREAM_FEEDBACK.md, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 24 doc-version targets] - Cross-repo: LLM-DocKit DF-029 + CONSENSUS_PROTOCOL_PROPOSAL.md normalised in the same date. - Version impact: yes (0.2.2 -> 0.2.3)
- 2026-05-03 - Claude Opus 4.7 (1M context) (proposer) + GPT-5 (critic) + Carlos (arbiter) - **Patch 0.2.2: Deployment Evidence Contract proposal + REVIEWS audit trail.** Consensus run on 2026-05-03 (5 rounds) produced two cross-repo proposals from a single audit signal: the protocol's `Service.interface` + tcp-probe support shipped in 0.2.0 / 0.2.1 (and on infra-portal repo at 0.8.0) but production was still running infra-portal:0.7.2, exposing that the protocol had no vocabulary to distinguish "implemented in repo" from "deployed and serving". Filed: (1) `docs/DEPLOYMENT_EVIDENCE_PROPOSAL.md` — self-contained proposal introducing a typed six-state lifecycle vocabulary (`declared / implemented / built / transferred / running / serving`), an explicit intent-vs-evidence rule (catalog declares intent, evidence is read by consumers from runtime, never edited into the catalog), and an optional `deployment` block on `Service` with nested `expected.image` and `expected.health` sub-blocks. Severity levels (INFO/WARN/FAIL) defined as semantics, not enforcement — consumers decide what action each level triggers. Five concrete homelab scenarios documented inline. Five anti-patterns named explicitly. The proposal does not implement schema; that is the next session's job, with full acceptance checklist included. (2) `docs/llm/REVIEWS.md` — structured audit-trail entry for the consensus run. Format follows the new `CONSENSUS_PROTOCOL_PROPOSAL.md` *Recording mechanism* in LLM-DocKit. Captures eight load-bearing decisions with causal reasoning (who proposed, which objection changed the resolution, why the resolution is correct, what risk was accepted) plus three explicit rejections. (3) `docs/DOWNSTREAM_FEEDBACK.md` DF-003 status → `accepted` with cross-reference to the proposal. (4) `docs/version-sync-manifest.yml` extended with the new proposal (28 markers). 28 doc-version targets synced via `scripts/bump-version.sh 0.2.2`. Cross-repo: sibling proposal `docs/CONSENSUS_PROTOCOL_PROPOSAL.md` filed in LLM-DocKit (same consensus run produced both); DF-029 in LLM-DocKit moved to `partially accepted` with cross-reference. ForgeOS mentioned in both proposals as future consumer / precedent, NOT as design requirement (rejected during the run by GPT-5: "no convertir ForgeOS en requisito formal"). - Files: [docs/DEPLOYMENT_EVIDENCE_PROPOSAL.md, docs/DOWNSTREAM_FEEDBACK.md, docs/llm/REVIEWS.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, CHANGELOG.md, docs/version-sync-manifest.yml, VERSION + 24 doc-version targets] - Version impact: yes (0.2.1 -> 0.2.2)
- 2026-05-02 - Claude Opus 4.7 (1M context) - Patch 0.2.1: matrix-only follow-up after `infra-portal` 0.8.0 (commit `717f468`) shipped both the consumer-side render-by-`interface` and the `node:net` TCP probe in the same minor bump. Updates: (1) `SPEC.md` *Consumer support for `interface`* matrix row for `infra-portal` now reads `0.8.0 / yes / yes` instead of `(pending)/(pending)/(pending)`; Notes column spells out the dispatch rules (`web` → open in new tab; `none` → silent no-op + toast; `api`/`mqtt`/`tcp`/`ssh`/`other` → clipboard copy + toast) and explicitly calls out that production still runs `infra-portal:0.7.2` until the operator promotes the image — adopters reading the matrix should not infer that production already supports the new values. (2) `docs/DOWNSTREAM_FEEDBACK.md` DF-002 moves from `partially implemented (protocol 0.2.0)` to `implemented (protocol 0.2.0 + infra-portal 0.8.0)`; DF-001 was already `implemented (0.2.0)`. (3) Open DF list now empty in HANDOFF — both inaugural entries cleared end-to-end through the channel installed in 0.1.6 (DF) → 0.2.0 (proposal+schema+SPEC) → 0.8.0 in the consumer → 0.2.1 (matrix sync). The DOWNSTREAM_FEEDBACK + PROPOSAL pattern survived its first real round-trip; this is the audit-trail-friendly evolution mechanism that 0.1.6 set out to build. 27 doc-version targets synced via `scripts/bump-version.sh 0.2.1`. - Files: [SPEC.md, docs/DOWNSTREAM_FEEDBACK.md, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 22 doc-version targets] - Version impact: yes (0.2.0 -> 0.2.1)
- 2026-05-02 - Claude Opus 4.7 (1M context) - Minor 0.2.0: shipped `Service.interface` per `docs/SERVICE_INTERFACE_PROPOSAL.md`. First additive schema field since 0.1.0; first concrete result of the `DOWNSTREAM_FEEDBACK.md` channel installed in 0.1.6. Changes: (1) `schemas/services.schema.json` adds `interface` (string, optional) on each service item with description naming the recommended enum (`web | api | mqtt | tcp | ssh | none | other`) and the rule "SHOULD be explicit when url is not http(s)://"; `additionalProperties: true` preserved so 0.1.x consumers ignore the new field. (2) `SPEC.md` *Service* section: added `interface` to Recommended fields, documented the seven recommended values, the implicit default (`web`), and the explicit-required rule when `url` is non-http; added new *Consumer support for `interface`* matrix as a permanent guardrail against the DF-002 class of bug. (3) `examples/home-infra/catalog/services.yml`: existing `infra` and `home-dashboard` entries gain `interface: web` (explicit-when-already-web pattern); two new sanitized entries `example-mqtt` (`mqtt://broker.example.internal:1883`, `interface: mqtt`, `status.type: tcp`) and `example-api` (`https://api.example.internal/v1`, `interface: api`). (4) `docs/PROJECT_CONTRACTS.md` notes that project-level `services[].interface` follows the same convention. (5) `docs/DOWNSTREAM_FEEDBACK.md` DF-001 → `implemented (0.2.0)`; DF-002 → `partially implemented (protocol 0.2.0)`. Acceptance checklist from the proposal cleared end-to-end; 27 doc-version targets synced via `scripts/bump-version.sh 0.2.0`. Consumer-side render + TCP probe will land in `infra-portal` and trigger a matrix-update patch on this repo. - Files: [schemas/services.schema.json, SPEC.md, examples/home-infra/catalog/services.yml, docs/PROJECT_CONTRACTS.md, docs/DOWNSTREAM_FEEDBACK.md, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 22 doc-version targets] - Version impact: yes (0.1.6 -> 0.2.0)
- 2026-05-02 - Claude Opus 4.7 (1M context) - Patch 0.1.6: install the missing protocol-evolution feedback channel after the first real-adopter session (`tomatic` v0.1.3) hit two protocol gaps simultaneously. Doc-only release; no SPEC or schema change. Added `docs/DOWNSTREAM_FEEDBACK.md` modelled on `LLM-DocKit:docs/DOWNSTREAM_FEEDBACK.md`: living DF-NNN log with Source / Date / Category / Status / Observation / Protocol implication, and a how-to-add-an-entry section. Two inaugural entries anchor the file with real adopter evidence: **DF-001** (`Service.url` is required and assumed-navigable, but `tomatic`'s decision to expose Mosquitto in the catalog produced `url: mqtt://...` which `infra-portal:App.tsx:149` cannot open via `window.open` — exposes the implicit "every service has a web UI" assumption that nothing in the SPEC actually states); **DF-002** (`status.type: "tcp"` is in `schemas/services.schema.json` but `infra-portal/src/server/health.ts:82-86` returns "not implemented yet" — the modal class "schema accepts X, consumer doesn't implement X"). Added `docs/SERVICE_INTERFACE_PROPOSAL.md` as the resolution path for DF-001: self-contained implementation brief proposing an additive optional `interface` field on `Service` with recommended enum `web | api | mqtt | tcp | ssh | none | other`, default `web` for backward compatibility, explicit value REQUIRED when `url` is not `http(s)://`. Includes file-by-file changes, acceptance checklist, migration path for `home-infra/catalog/services.yml`, and consumer-side responsibilities for `infra-portal`. The proposal also recommends adding a "Consumer support matrix" section to `SPEC.md` as the permanent guardrail against the DF-002 class. Both new documents added to `docs/version-sync-manifest.yml` (27 markers total). 27 doc-version targets synced via `scripts/bump-version.sh 0.1.6`. Rationale for shipping the channel doc-only before the schema change: the next session implementing `interface` should be able to read the proposal cold and ship without needing this conversation; that property requires the proposal document to exist, not the schema change to be premature. The actual additive schema change ships in 0.2.0 when a future session executes the proposal's acceptance checklist. - Files: [docs/DOWNSTREAM_FEEDBACK.md, docs/SERVICE_INTERFACE_PROPOSAL.md, docs/version-sync-manifest.yml, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 23 doc-version targets] - Version impact: yes (0.1.5 -> 0.1.6)
- 2026-05-01 - Claude - Patch 0.1.5: three GPT-5-review fixes against the freshly published 0.1.4. (1) `integrations/dockit/new-homelab-project.sh` was splitting `--description` with spaces into multiple positional args to `gh repo create` because `DESC_ARG="--description $DESCRIPTION"` was constructed and expanded unquoted. Rewritten to use POSIX positional parameters: `set -- "$GH_OWNER/$PROJECT_NAME" "--$VISIBILITY" "--source=$TARGET_DIR" --remote=origin --push` then conditionally `set -- "$@" --description "$DESCRIPTION"` if non-empty, then `gh repo create "$@"`. Verified mechanically by stubbing `gh` with a printer: a description with internal spaces survives as a single argv element (argv[7] = the whole description). (2) `integrations/dockit/checklists/PROJECT_CHECKLIST.md` previously listed `docker compose restart edge-caddy` or `caddy reload` as equivalent options for the edge-caddy reload step. Both wrong for QNAP NAS — `docker compose` subcommand is not on PATH there per `home-infra/docs/CONVENTIONS.md`, and `caddy reload` may not pick up newly-added vhosts depending on how Caddy was started. Replaced with the full compose-plugin path (`/usr/local/lib/docker/cli-plugins/docker-compose -f /share/Container/compose/edge-caddy/docker-compose.yml restart edge-caddy`) and a note preferring `restart` over `reload` for new vhosts. (3) `integrations/dockit/templates/AGENTS.md` mandated PROJECTS.md updates "whenever a project is created, bumps version, or changes status" — too aggressive for projects with frequent internal patches. Tightened to **deployed-reality** scope: project created or retired, deployed version on a host changes, status / host / exposure / URL changes. Internal patches that never reach a host do NOT warrant a PROJECTS.md update — the table tracks observed deployed state, not repo HEAD. End-to-end smoke test against `/tmp/smoke-homelab` confirmed: branch is `main`, AGENTS.md reflects the new wording, checklist reflects the QNAP path. The fix in (1) pairs with LLM-DocKit v4.6.1 which fixed the master/main default-branch issue in `dockit-init-project.sh`. 25 doc-version targets synced via `scripts/bump-version.sh 0.1.5`. - Files: [integrations/dockit/new-homelab-project.sh, integrations/dockit/checklists/PROJECT_CHECKLIST.md, integrations/dockit/templates/AGENTS.md, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 21 doc-version targets] - Version impact: yes (0.1.4 -> 0.1.5)
- 2026-05-01 - Claude - Patch 0.1.4: orchestrator + Claude skill for end-to-end "new homelab project" UX. Closes the loop on the operator's UX target: from `~/src/`, tell the LLM to start a new project, everything happens with one confirmation. Added `integrations/dockit/new-homelab-project.sh` — POSIX sh orchestrator that calls `cdelalama/LLM-DocKit:scripts/dockit-init-project.sh` for the generic scaffold, then `apply-profile.sh` for the homelab layer, then optionally `gh repo create … --source=… --push` when `--github` is passed. GitHub creation is opt-in (effects visible to others stay explicit). Aborts cleanly if target dir exists or if `cdelalama/<name>` is already taken on GitHub. Smoke-tested end-to-end against `/tmp/smoke-homelab`: validator 6/6 PASS, two-commit history (initial scaffold + apply homelab profile), AGENTS.md + CLAUDE.md symlink present, infra.contract.yml + checklist installed, "Suggested row for ~/src/home-infra/docs/PROJECTS.md" printed at the end. The orchestrator deliberately does NOT edit `~/src/home-infra/` itself — that is the skill's job because PROJECTS.md is a structured table and an LLM with the Edit tool is safer than awk/sed. Added `integrations/dockit/skills/new-homelab-project/SKILL.md` — the Claude Code skill that wraps the orchestrator with a five-question conversation (name, description, host, exposes-UI, GitHub now?), prints a literal plan, waits for confirmation, runs the orchestrator with the matching flags, then edits `~/src/home-infra/docs/PROJECTS.md` (Active Projects row + Project Details subsection) and commits + pushes in home-infra as a separate commit. Failure modes documented in the skill: target exists / GitHub name taken / gh not authenticated / partial-success after local steps — all leave the local repo intact, none trigger destructive cleanup. `INTEGRATION.md` rewritten to document the three entry points (orchestrator, skill, profile-only) and a new "Layering and single source of truth" section emphasising that all three converge on the same `apply-profile.sh` so profile logic cannot drift. One-time setup for the operator: a single `ln -s ~/src/home-infra-protocol/integrations/dockit/skills/new-homelab-project ~/.claude/skills/new-homelab-project` makes `/new-homelab-project` discoverable from any Claude Code session anywhere. 25 doc-version targets synced via `scripts/bump-version.sh 0.1.4`. - Files: [integrations/dockit/new-homelab-project.sh, integrations/dockit/skills/new-homelab-project/SKILL.md, integrations/dockit/INTEGRATION.md, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 21 doc-version targets] - Version impact: yes (0.1.3 -> 0.1.4)
- 2026-05-01 - Claude - Patch 0.1.3: homelab profile for LLM-DocKit projects. Added `integrations/dockit/` with five files: `INTEGRATION.md` (design + how to apply + multi-LLM rationale), `templates/AGENTS.md` (LLM-neutral agent context), `templates/infra.contract.yml` (protocol-aligned template with TODO placeholders, marked experimental), `checklists/PROJECT_CHECKLIST.md` (operational deploy checklist covering build, secrets, runtime, network/TLS, source-of-truth updates, project docs, contract, smoke checks), and `apply-profile.sh` (POSIX sh, idempotent, never overwrites; symlinks CLAUDE.md → AGENTS.md so Claude Code's loader picks up the same content without duplicating it). Smoke-tested locally on a temp dir: fresh apply created 4 files including the symlink; re-run skipped all 4 cleanly. The profile lives in this repo (not in LLM-DocKit) on purpose, so LLM-DocKit stays general-purpose; the homelab-specific layer is owned by the protocol that knows what "participate in the homelab" means. `integrations/dockit/INTEGRATION.md` added to `docs/version-sync-manifest.yml`. 25 doc-version targets synced via `scripts/bump-version.sh 0.1.3`. - Files: [integrations/dockit/INTEGRATION.md, integrations/dockit/templates/AGENTS.md, integrations/dockit/templates/infra.contract.yml, integrations/dockit/checklists/PROJECT_CHECKLIST.md, integrations/dockit/apply-profile.sh, docs/version-sync-manifest.yml, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 21 doc-version targets] - Version impact: yes (0.1.2 -> 0.1.3)
- 2026-05-01 - Claude - Patch 0.1.2: README Ecosystem map. Added an explicit "Ecosystem map" section between Overview and Quick Start listing the four homelab repositories alongside this one (LLM-DocKit / home-infra-protocol / home-infra / infra-portal / infra-agent) with role, visibility and status per repo. The explanatory paragraph above the table states why visibility differs: source-of-truth and consumer repositories are private because they contain real hosts, IPs, runbook references, and secret store names; the protocol stays public so others can adopt or learn from the same contracts. The map also clarifies that LLM-DocKit is kept separate from this protocol on purpose so it can stay general-purpose; new ecosystem projects scaffold from LLM-DocKit first per docs/GOVERNANCE.md Project Bootstrap Rule and may opt into the protocol's contracts as they mature. 24 doc-version targets synced via `scripts/bump-version.sh 0.1.2`. - Files: [README.md, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION + 20 doc-version targets] - Version impact: yes (0.1.1 -> 0.1.2)
- 2026-05-01 - Codex - Added protocol governance rules: anti-YAGNI field policy, ownership boundaries across protocol/source-of-truth/portal/telemetry provider roles, LLM-DocKit bootstrap rule for new ecosystem projects, and compliance-claim freshness requirements. Linked the new governance doc from the README, spec, usage guide, start-here guide, and structure map. - Files: [docs/GOVERNANCE.md, README.md, SPEC.md, HOW_TO_USE.md, LLM_START_HERE.md, docs/STRUCTURE.md, docs/version-sync-manifest.yml, CHANGELOG.md, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, VERSION] - Version impact: yes (0.1.0 -> 0.1.1)
- 2026-05-01 - Codex - Created `home-infra-protocol` from the LLM-DocKit scaffold. Rewrote project docs for a protocol/specification repo, added `SPEC.md`, initial JSON Schema drafts, sanitized examples, and docs for completion, security, recovery, LLM workflow, and project contracts. - Files: [README.md, LLM_START_HERE.md, HOW_TO_USE.md, SPEC.md, docs/PROJECT_CONTEXT.md, docs/ARCHITECTURE.md, docs/STRUCTURE.md, docs/COMPLETION_RULE.md, docs/PROJECT_CONTRACTS.md, docs/SECURITY_MODEL.md, docs/RECOVERY_MODEL.md, docs/LLM_WORKFLOW.md, schemas/*.json, examples/**, docs/llm/HANDOFF.md, docs/llm/HISTORY.md, docs/llm/DECISIONS.md, CHANGELOG.md, VERSION] - Version impact: yes (initial 0.1.0)
