# Example Project Infra Contract

This project participates in an infrastructure source-of-truth repo by exposing
`infra.contract.yml`.

The contract names services, runbooks, and secret references. It does not
contain secret values and it is not authoritative until a source-of-truth repo
ingests or copies it.
